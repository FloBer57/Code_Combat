# Une tombe oubliée dans la forêt!
# Mais les ogres sont sur tes talons.
# Brise la tombe, pendant que tu te défends des mioches.

# Cette fonction doit attaquer un ennemi s'il existe, sinon attaque la porte!
def checkToDefend(target):
    # Vérifie si  `target` existe.
    
        # Si c'est le cas, attaque `target`.
        
    # Utilise un else pour faire quelque chose si il n'y a pas de `target` (la variable target est vide)
    
        # Sinon attaque la porte ("Door").
        
    pass

while True:
    enemy = hero.findNearestEnemy()
    checkToDefend(enemy)
